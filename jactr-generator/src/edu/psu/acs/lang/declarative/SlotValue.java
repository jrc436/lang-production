package edu.psu.acs.lang.declarative;

public interface SlotValue {
	public String toString(); //technically unnecessary, this interface is just for documentation and extension purposes
						      //mostly to avoid generics on something that isn't *really* generic
}
